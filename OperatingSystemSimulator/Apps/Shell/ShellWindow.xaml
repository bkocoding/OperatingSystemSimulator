<UserControl
    x:Class="OperatingSystemSimulator.Apps.Shell.ShellWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OperatingSystemSimulator.Apps.Shell"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="30" PointerPressed="UserControl_PointerPressed">

  <Grid>
    <StackPanel Grid.Row="0" Background="Gray" PointerPressed="Pointer_Pressed" BorderBrush="Black" BorderThickness="1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="1" x:Name="titleText" VerticalAlignment="Center" Margin="5" HorizontalAlignment="Center" TextWrapping="Wrap" MaxLines="1"/>
        <Button x:Name="Accessibility_button" Grid.Column="3" Content="?" HorizontalAlignment="Right" Background="Transparent" BorderBrush="{x:Null}"/>
        <Button Grid.Column="4" Content="X" HorizontalAlignment="Right" Background="Transparent" BorderBrush="{x:Null}" Click="Button_Click"/>
      </Grid>
    </StackPanel>
  </Grid>
</UserControl>
