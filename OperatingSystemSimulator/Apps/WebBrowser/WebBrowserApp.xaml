<UserControl
    x:Class="OperatingSystemSimulator.Apps.WebBrowser.WebBrowserApp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OperatingSystemSimulator.Apps.WebBrowser"
    xmlns:pages="using:OperatingSystemSimulator.Apps.WebBrowser.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shell="using:OperatingSystemSimulator.Apps.Shell"
    mc:Ignorable="d"
    PointerPressed="UserControl_PointerPressed">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <shell:ShellWindow x:Name="ShellTitleBar" Grid.Row="0" Title="{Binding CurrentTitle}"/>

    <Border Background="#424651" BorderBrush="Black" BorderThickness="1" Width="700" Height="400" Grid.Row="1">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="41">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <StackPanel Grid.Column="0" Orientation="Horizontal">
            <Button x:Name="BackButton" Content="←" VerticalAlignment="Center" Margin="4,0,4,0" Foreground="#69beff" Height="33" Width="40" FontSize="20" Click="BackButton_Click" Background="Transparent" BorderBrush="#69beff" IsEnabled="{Binding IsBackButtonEnabled}"/>
            <Button x:Name="ForwardButton" Content="→" VerticalAlignment="Center" Margin="4,0,4,0" Foreground="#69beff" Height="33" Width="40" FontSize="20" Click="ForwardButton_Click" Background="Transparent" BorderBrush="#69beff" IsEnabled="{Binding IsForwardButtonEnabled}"/>
            <Button x:Name="RefreshButton" Content="↻" VerticalAlignment="Center" Margin="4,0,4,0" Foreground="#69beff" Height="33" Width="40" FontSize="20" Click="RefreshButton_Click" Background="Transparent" BorderBrush="#69beff"/>
            <Button x:Name="HomeButton" Content="⌂" VerticalAlignment="Center" Margin="4,0,4,0" Foreground="#69beff" Height="33" Width="40" FontSize="20" Click="HomeButton_Click" Background="Transparent" BorderBrush="#69beff"/>
          </StackPanel>
          <TextBox x:Name="AddressBarTextBox" Grid.Column="1" VerticalAlignment="Center" Background="#383838" Foreground="White" Height="30" Width="Auto" Margin="4,0,4,0" PlaceholderText="Type an adress" Text="{Binding CurrentAddress}" KeyDown="AddressBarTextBox_KeyDown"/>
          <Button x:Name="GoButton" Grid.Column="2" Content="Go" VerticalAlignment="Center" Margin="4,0,4,0" Foreground="#69beff" Height="33" Width="40" Click="GoButton_Click" Background="Transparent" BorderBrush="#69beff"/>
        </Grid>

        <ContentControl x:Name="PageContentControl" Content="{Binding CurrentPage}" Grid.Row="1"/>


      </Grid>
    </Border>
  </Grid>

</UserControl>
