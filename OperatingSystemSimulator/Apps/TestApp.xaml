<UserControl
    x:Class="OperatingSystemSimulator.Apps.TestApp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:shell="using:OperatingSystemSimulator.Apps.Shell"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="400" PointerPressed="UserControl_PointerPressed">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <shell:ShellWindow x:Name="ShellTitleBar" Grid.Row="0"/>

    <Border Background="#2E2E2E" BorderBrush="Black" BorderThickness="1" Width="200" Height="200" Grid.Row="1">
      <TextBlock x:Name="ContentText" Margin="10" Foreground="White"/>
    </Border>
  </Grid>
</UserControl>
