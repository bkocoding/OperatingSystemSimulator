<Page
    x:Class="OperatingSystemSimulator.Pages.BugCheckPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OperatingSystemSimulator.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#095fa1">

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10">
      <TextBlock x:Name="BugCheckText" FontSize="20" Foreground="White" TextAlignment="Left" />
    </StackPanel>

    <ProgressBar x:Name="BugCheckProgress" Grid.Column="0" Grid.Row="1" Width="Auto" Height="20" Minimum="0" Maximum="100" VerticalAlignment="Bottom" Margin="0,0,0,10"/>
  </Grid>
</Page>
